<section id="form">
  <h2 class="mt-3">Tambah Data</h2>
  <div class="row">
    <div class="col-md-6">
      <% if (typeof errors != 'undefined') { %>
      <div class="alert alert-danger" role="alert">
        <ul>
          <% errors.forEach(err => { %>
          <li><%= err.msg %></li>
          <% }) %>
        </ul>
      </div>
      <% } %>
      <form action="/contact" method="post">
        <div class="mb-3">
          <label for="nama" class="form-label">Nama Lengkap</label>
          <input type="text" class="form-control" id="nama" required autofocus name="nama" />
        </div>
        <div class="mb-3">
          <label for="Email" class="form-label">Email</label>
          <input type="email" class="form-control" id="email" required name="email" />
        </div>
        <div class="mb-3">
          <label for="No Hp" class="form-label">No Hp</label>
          <input type="text" class="form-control" id="noHp" required name="nohp" />
        </div>
        <button type="submit" class="btn btn-warning">Add contact</button>
      </form>
    </div>
  </div>
</section>
